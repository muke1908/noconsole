{
  "name": "noconsole",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=packages/streamer",
    "start": "npm run start --workspace=packages/streamer",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.28.5",
    "ts-jest": "^29.4.6"
  }
}
